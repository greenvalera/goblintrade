const initialState = {
    user: {
        firstName: null,
        lastName: null,
    }
};

export const authReducer = (state = initialState, action) => {
    return state;
};

export default authReducer;